// functional_mod.jawa — Module fixture kanggo test higher-order function
gawe faktor = 10

ekspor fungsi kaliFaktor(x) {
    bali x * faktor
}

ekspor fungsi kuadrat(x) {
    bali x * x
}

ekspor fungsi transformasiData(fn, arr) {
    bali terapkan(fn, arr)
}
